{
  "name": "dead-drop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types src/roundtrip.ts"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3"
  }
}
